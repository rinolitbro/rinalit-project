using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RinolitSodaTrigger : MonoBehaviour
{
    public Animator Rinolit;

    // Start is called before the first frame update
    void OnTriggerEnter()
    {
        if (GameObject.Find("Baldi"))
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
